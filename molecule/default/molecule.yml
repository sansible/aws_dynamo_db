---

driver:
  name: docker

platforms:
  - name: aws-dynamo-db-xenial
    image: ubuntu:xenial
  - name: aws-dynamo-db-bionic
    image: ubuntu:bionic

provisioner:
  name: ansible
  config_options:
    defaults:
      hash_behaviour: merge
  lint:
    name: ansible-lint

lint:
  name: yamllint
  options:
    config-data:
      extends: default
      ignore: .venv_ansible*
      rules:
        line-length:
          max: 159
        truthy: disable

dependency:
  name: galaxy

scenario:
  name: default

verifier:
  name: testinfra
  lint:
    name: flake8
